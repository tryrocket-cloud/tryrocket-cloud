apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rgit-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: longhorn-retain
  volumeName: rgit-pv