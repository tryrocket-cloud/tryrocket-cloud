apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: hetzner-subaccount-ssh-privatekey-external-secret
spec:
  refreshInterval: "7d"
  secretStoreRef:
    name: vault-cluster-secrets-store-kv2
    kind: ClusterSecretStore
  target:
    name: hetzner-subaccount-ssh-privatekey
    creationPolicy: Owner
  data:
    - secretKey: ssh-privatekey
      remoteRef:
        key: ssh/tryrocket.cloud/vaultwarden
        property: hetzner-subaccount-ssh-privatekey
