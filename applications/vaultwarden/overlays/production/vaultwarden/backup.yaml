apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: vaultwarden-daily-backup
spec:
  schedule: "*/5 * * * *"
  template:
    ttl: 720h
    includedNamespaces:
      - vaultwarden
    snapshotVolumes: true
