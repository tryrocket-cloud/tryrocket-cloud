apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: vaultwarden-daily-backup
spec:
  schedule: "* * * * *"
  template:
    ttl: 720h
    includedNamespaces:
      - vaultwarden
    snapshotVolumes: true
