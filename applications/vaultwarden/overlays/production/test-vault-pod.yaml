apiVersion: v1
kind: Pod
metadata:
  name: example-pod
  annotations:
    vault.hashicorp.com/agent-inject: true
    vault.hashicorp.com/role: "myapp-role"
    vault.hashicorp.com/agent-inject-secret-hello: "kv/data/myapp"
spec:
  serviceAccountName: myapp-role
  containers:
  - name: test-image
    image: alpine
    command: 
      - bin
      - sh
      - |
        cat /vault/secrets/hello
        sleep infinity
#    volumeMounts:
#    - name: vault-secrets
#      mountPath: /vault/secrets
#  volumes:
#  - name: vault-secrets
#    emptyDir: {}