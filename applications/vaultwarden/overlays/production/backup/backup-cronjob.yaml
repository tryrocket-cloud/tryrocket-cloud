apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup
spec:
  suspend: false
  schedule: "30 0 * * *"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        spec:
          restartPolicy: Never

          initContainers:
            - name: get-vaultwarden-version
              image: ghcr.io/tryrocket-cloud/tryrocket-cloud:backup
              command:
                - /bin/sh
                - -c
                - |
                  apk add --no-cache curl

                  curl --silent https://vaultwarden.tryrocket.cloud/api/config | jq --raw-output '.server.version' > /shared/vaultwarden.env
              volumeMounts:
              - name: vaultwarden-version-volume
                mountPath: /shared

            - name: scale-down
              image: bitnami/kubectl:latest
              command:
              - /bin/sh
              - -c
              - |
                # sleep infinity
                kubectl scale deployment vaultwarden --replicas=0 -n vaultwarden

                while [ "$(kubectl get pods -l app=vaultwarden -n vaultwarden --field-selector=status.phase=Running | wc -l)" != "0" ]; do
                  echo "Waiting for Vaultwarden pods to terminate..."
                  sleep 5
                done

          containers:

            - name: ionos-cloud-objectstorage-eu-central-1-s3-restic-backup
              image: ghcr.io/tryrocket-cloud/tryrocket-cloud:backup
              imagePullPolicy: Always
              envFrom:
                - secretRef:
                    name: ionos-cloud-objectstorage-s3-secret
                - secretRef:
                    name: ionos-cloud-objectstorage-eu-central-1-restic-secret
                - secretRef:
                    name: ionos-cloud-objectstorage-eu-central-1-healthchecks-secret
              command:
                  - /bin/sh
                  - -c
                  - |
                    export VAULTWARDEN_VERSION=$(cat /shared/vaultwarden.env)
                    sh backup.sh

              volumeMounts:
                - name: backup-sh-volume
                  mountPath: /backup.sh
                  subPath: backup.sh
                - name: vaultwarden-data-volume
                  mountPath: /data
                  readOnly: true
                - name: vaultwarden-version-volume
                  mountPath: /shared
                  readOnly: true

            - name: ionos-cloud-objectstorage-eu-central-3-s3-restic-backup
              image: ghcr.io/tryrocket-cloud/tryrocket-cloud:backup
              imagePullPolicy: Always
              envFrom:
                - secretRef:
                    name: ionos-cloud-objectstorage-s3-secret
                - secretRef:
                    name: ionos-cloud-objectstorage-eu-central-3-restic-secret
                - secretRef:
                    name: ionos-cloud-objectstorage-eu-central-3-healthchecks-secret
              command:
                  - /bin/sh
                  - -c
                  - |
                    export VAULTWARDEN_VERSION=$(cat /shared/vaultwarden.env)
                    sh backup.sh

              volumeMounts:
                - name: backup-sh-volume
                  mountPath: /backup.sh
                  subPath: backup.sh
                - name: vaultwarden-data-volume
                  mountPath: /data
                  readOnly: true
                - name: vaultwarden-version-volume
                  mountPath: /shared
                  readOnly: true

            - name: hetzner-storagebox-bx11-fsn1-sftp-restic-backup
              image: ghcr.io/tryrocket-cloud/tryrocket-cloud:backup
              imagePullPolicy: Always
              envFrom:
                - secretRef:
                    name: hetzner-storagebox-bx11-fsn1-restic-secret
                - secretRef:
                    name: hetzner-storagebox-bx11-fsn1-healthchecks-secret
              command:
                  - /bin/sh
                  - -c
                  - |
                    export VAULTWARDEN_VERSION=$(cat /shared/vaultwarden.env)
                    ssh-keyscan -p 23 -f /root/.ssh/hosts >> /root/.ssh/known_hosts

                    sh backup.sh
              volumeMounts:
                - name: backup-sh-volume
                  mountPath: /backup.sh
                  subPath: backup.sh
                - name: vaultwarden-data-volume
                  mountPath: /data
                  readOnly: true
                - name: hetzner-storagebox-vaultwarden-ssh-privatekey
                  mountPath: /root/.ssh/id_ed25519
                  readOnly: true
                  subPath: id_ed25519
                - name: hetzner-storagebox-vaultwarden-ssh-config
                  mountPath: /root/.ssh/config
                  readOnly: true
                  subPath: config
                - name: hetzner-storagebox-vaultwarden-ssh-hosts
                  mountPath: /root/.ssh/hosts
                  readOnly: true
                  subPath: hosts
                - name: vaultwarden-version-volume
                  mountPath: /shared
                  readOnly: true

            - name: tryrocketcloud-truenas-h610i-sftp-restic-backup
              image: ghcr.io/tryrocket-cloud/tryrocket-cloud:backup
              imagePullPolicy: Always
              envFrom:
                - secretRef:
                    name: tryrocketcloud-truenas-h610i-restic-secret
                - secretRef:
                    name: tryrocketcloud-truenas-h610i-healthchecks-secret
              command:
                  - /bin/sh
                  - -c
                  - |
                    ssh-keyscan -f /root/.ssh/hosts >> /root/.ssh/known_hosts

                    export VAULTWARDEN_VERSION=$(cat /shared/vaultwarden.env)
                    sh backup.sh
              volumeMounts:
                - name: backup-sh-volume
                  mountPath: /backup.sh
                  subPath: backup.sh
                - name: vaultwarden-data-volume
                  mountPath: /data
                  readOnly: true
                - name: tryrocketcloud-truenas-vaultwarden-ssh-privatekey
                  mountPath: /root/.ssh/id_ed25519
                  readOnly: true
                  subPath: id_ed25519
                - name: tryrocketcloud-truenas-vaultwarden-ssh-config
                  mountPath: /root/.ssh/config
                  readOnly: true
                  subPath: config
                - name: tryrocketcloud-truenas-vaultwarden-ssh-hosts
                  mountPath: /root/.ssh/hosts
                  readOnly: true
                  subPath: hosts
                - name: vaultwarden-version-volume
                  mountPath: /shared
                  readOnly: true
                  
          volumes:

            - name: backup-sh-volume
              configMap:
                name: backup-sh-configmap
                items:
                - key: backup.sh
                  path: backup.sh

            - name: vaultwarden-data-volume
              persistentVolumeClaim:
                claimName: vaultwarden-data-pvc

            - name: hetzner-storagebox-vaultwarden-ssh-privatekey
              secret:
                secretName: hetzner-storagebox-bx11-fsn1-sftp-secret
                items:
                  - key: ssh-privatekey
                    path: id_ed25519
                defaultMode: 384
            - name: hetzner-storagebox-vaultwarden-ssh-config
              secret:
                secretName: hetzner-storagebox-bx11-fsn1-sftp-secret
                items:
                  - key: ssh-config
                    path: config
                defaultMode: 384
            - name: hetzner-storagebox-vaultwarden-ssh-hosts
              secret:
                secretName: hetzner-storagebox-bx11-fsn1-sftp-secret
                items:
                  - key: ssh-hosts
                    path: hosts
                defaultMode: 384

            - name: tryrocketcloud-truenas-vaultwarden-ssh-privatekey
              secret:
                secretName: tryrocketcloud-truenas-h610i-sftp-secret
                items:
                  - key: ssh-privatekey
                    path: id_ed25519
                defaultMode: 384
            - name: tryrocketcloud-truenas-vaultwarden-ssh-config
              secret:
                secretName: tryrocketcloud-truenas-h610i-sftp-secret
                items:
                  - key: ssh-config
                    path: config
                defaultMode: 384
            - name: tryrocketcloud-truenas-vaultwarden-ssh-hosts
              secret:
                secretName: tryrocketcloud-truenas-h610i-sftp-secret
                items:
                  - key: ssh-hosts
                    path: hosts
                defaultMode: 384
            - name: vaultwarden-version-volume
              emptyDir: {}