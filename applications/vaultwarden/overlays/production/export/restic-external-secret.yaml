apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: restic-repository-external-secret
spec:
  refreshInterval: "168h"
  secretStoreRef:
    name: vault
    kind: SecretStore
  target:
    name: restic-repository-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: vaultwarden.tryrocket.cloud/backup/restic
