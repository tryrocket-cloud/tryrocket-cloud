apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: vaultwarden-preview
spec:
  generators:
    - pullRequest:
        github:
          owner: tryrocket-cloud
          repo: tryrocket-cloud
          labels:
            - preview
            - vaultwarden
            - update
        requeueAfterSeconds: 60
  template:
    metadata:
      name: '{{branch}}-preview'
      labels:
        environment: preview
        app.kubernetes.io/part-of: "Rocket Cloud"
        app.kubernetes.io/managed-by: argocd
    spec:
      project: "default"
      source:
        repoURL: 'https://github.com/tryrocket-cloud/tryrocket-cloud.git'
        targetRevision: '{{branch}}'
        path: applications/vaultwarden/overlays/preview

        # TODO: maybe read kustomization.yaml insetad
        kustomize:
          nameSuffix: 'HAHAH-suffix'
#          resources:
#            - namespace.yaml
#            - ../../base/vaultwarden
#            - ../../base/postgres

#          patches:
#            - target:
#                kind: Ingress
#                name: vaultwarden
#              patch: |
#                - op: replace
#                  path: /spec/tls/0/hosts/0
#                  value: '{{.branch_slug}}-preview.tryrocket.cloud'
#                - op: replace
#                  path: /spec/rules/0/host
#                  value: '{{.branch_slug}}-preview.tryrocket.cloud'
#            - target:
#                kind: Deployment
#                name: simple-deployment
#              patch: |-
#                - op: replace
#                  path: /spec/template/spec/containers/0/env/0
#                  value:
#                    name: GIT_BRANCH
#                    value: '{{.branch_slug}}-preview'
#                - op: replace
#                  path: /spec/template/spec/containers/0/env/1
#                  value:
#                    name: GIT_HASH
#                    value: '{{.head_short_sha_7}}'
      destination:
        server: 'https://192.168.178.101:6443'
        namespace: '{{branch}}-preview'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true