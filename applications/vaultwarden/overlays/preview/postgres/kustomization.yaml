apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/postgres

patches:
  - path: patch-deployment.yaml

# POSTGRES_HOST is set dynamically by application-set.yaml

secretGenerator:
  - name: postgres-secret
    literals:
#      - POSTGRES_USER=vaultwarden # TODO: adjust, maybe wrong after pg_restore
#      - POSTGRES_PASSWORD=vaultwarden
