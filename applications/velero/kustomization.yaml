apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - bsl.yaml
  - bsl2.yaml

  - vsl.yaml

  - velero-repo-credentials-externalsecret.yaml

  - ./external-secrets

namespace: velero