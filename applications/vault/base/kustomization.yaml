apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # helm repo add hashicorp https://helm.releases.hashicorp.com
  # helm repo update
  # helm template hashicorp/vault --version 0.28.1 --values helm-vault-values.yaml > vault-helm-0.28.1.yaml
  - vault-helm-0.28.1.yaml
