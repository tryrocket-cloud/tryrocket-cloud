apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-home-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: longhorn-retain
  volumeName: adguard-home-pvc