apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - configmap.yaml

configMapGenerator:
  - name: homer-config
    files:
      - config.yml=config.yml
      - logo.png=logo.png