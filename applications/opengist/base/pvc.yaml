apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opengist-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn-retain
  volumeName: opengist-pv