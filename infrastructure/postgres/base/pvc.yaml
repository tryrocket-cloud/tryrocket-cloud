apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-16-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 16Gi
  storageClassName: longhorn-retain
  volumeName: postgres-16-pv