apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base

# TODO: include patch here inside without extra files
patches:
  - path: patch-application.yaml
    target:
      kind: Application
  - path: patch-certificate.yaml
    target:
      kind: Certificate